{
  "name": "enhanced-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Runnable enhanced Agent example for chapter 16",
  "scripts": {
    "build": "tsc",
    "start": "npm run build --silent && node dist/main.js",
    "test": "npm run build --silent && node dist/test.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}

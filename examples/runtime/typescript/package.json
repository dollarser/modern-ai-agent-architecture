{
  "name": "agent-runtime",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Runnable Agent Runtime state machine for chapter 9",
  "scripts": {
    "build": "tsc",
    "start": "npm run build --silent && node dist/main.js",
    "test": "npm run build --silent && node dist/test.js"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
